// src/__tests__/setup.js
// This file extends Jest config for specific React Native & Expo test cases

// Add any additional test-specific mocks here
// These will not override any mocks defined in jest.setup.js

// Optional: Add custom test renders or test utilities

// Dummy assertion to make sure the setup file runs
test('setup file runs', () => {
  expect(true).toBe(true);
});
